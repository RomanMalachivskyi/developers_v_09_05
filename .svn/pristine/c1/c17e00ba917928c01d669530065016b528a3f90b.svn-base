package com.epam.lab.developers.tests;
import static org.junit.Assert.*;

import net.sourceforge.jwebunit.junit.WebTester;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;


public class HomeController {
	private WebTester tester;
	
    @Before
    public void prepare() {
        tester = new WebTester();
        tester.setBaseUrl("http://localhost:8080/developers");
    }

	@Test
	public void test() {
		
		tester.beginAt("/home"); //Open the browser on http://localhost:8080/test/home.xhtml
		//tester.clickLink("btnLogin");
		tester.assertButtonPresent("s_Login");	//чи існує кнопка з ід s_Login
		tester.assertButtonPresent("s_Register");	//чи існує кнопка з ід s_Login
		tester.assertElementPresent("name");		//чи існує поле з ід name	
		tester.setTextField("name", "aquilla");
		tester.setTextField("password", "12345");
		tester.setTextField("r_password", "12345");
		tester.setTextField("email", "aquilla@i.ua");
		//tester.clickButton("s_Register");
		tester.submit("s_Register");
		//tester.assertElementPresent("errorName");
	
	}
	
	
	@After
	public void tearDown() throws Exception {
		tester.assertTextPresent("Such login already exists");	
	}

}
